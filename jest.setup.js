// Global Jest setup for the Expo app.
//
// React Native Testing Library (>=13) auto-registers its jest matchers when
// imported in a test file, so no extend-expect import is needed here.
//
// Per-test mocks (socket singleton, API services, native modules) live in the
// individual test files that need them, to keep mocking explicit and local.
