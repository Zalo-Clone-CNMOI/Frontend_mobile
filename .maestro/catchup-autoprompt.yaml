appId: com.x22664321k.Zalo_2026
---
# E2E (Issue #5): a catch-up summary is auto-injected into the Zai conversation.
#
# Pre-conditions: logged in; on the Messages/home tab; at least one conversation
# has unread messages.
#
# NOTE: selectors below are best-effort. Adjust the conversation-row / "Tóm tắt"
# matchers to the real testIDs/labels rendered by app/(tabs)/home.tsx before
# running on a device/emulator.
- launchApp
- longPressOn:
    text: ".*"          # a conversation list row with unread messages
- tapOn: "Tóm tắt"        # catch-up action in the long-press menu
# App navigates to the Zai assistant chat and auto-sends the summary as a message.
- assertVisible: "Zai"
- assertVisible:
    text: ".+"           # the catch-up summary appears as a sent message bubble
